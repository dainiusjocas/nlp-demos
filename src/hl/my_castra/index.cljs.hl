(page "index.html"
  (:require
    [my-castra.rpc :as rpc]
    [my-castra.header :as header]))

(enable-console-print!)

(html
  (head
    (link :href "app.css" :rel "stylesheet"))
  (body
    (header/header)
    (h1 (text "Demo app, made with Hoplon and Castra!"))))
